
const APP_DATA = {"rows":[{"ski":"Stöckli Montero AX (173cm)","length":173,"base":8.8,"forms":[7.0,8.0,9.0,9.0,10.0],"snows":[9.0,8.0,8.0,8.0,9.0,7.0,8.0,7.0],"style1":8.0,"style2":9.0,"skills":[3.0,8.0,10.0,9.0,8.0],"radius":15.5,"tip":123,"mid":80,"tail":112,"link":"https://www.stoeckli.ch/usen/ski/ski/allround/montero-ax"},{"ski":"Stöckli Montero AR (175cm)","length":175,"base":8.6,"forms":[5.0,6.0,8.0,10.0,7.0],"snows":[10.0,9.0,8.0,6.0,7.0,6.0,6.0,5.0],"style1":9.0,"style2":6.0,"skills":[2.0,6.0,8.0,9.0,9.0],"radius":16.3,"tip":128,"mid":84,"tail":114,"link":"https://www.stoeckli.ch/usen/ski/ski/allround/montero-ar"},{"ski":"Head e-Rally (170cm)","length":170,"base":8.5,"forms":[6.0,8.0,9.0,8.0,8.0],"snows":[9.0,8.0,8.0,8.0,8.0,6.0,7.0,6.0],"style1":8.0,"style2":8.0,"skills":[3.0,8.0,9.0,9.0,8.0],"radius":14.0,"tip":128,"mid":78,"tail":112,"link":"https://www.freeride.se/prylar/skidor/head-supershape-e-rally.html"},{"ski":"Völkl Deacon 84 (177cm)","length":177,"base":8.2,"forms":[6.0,7.0,8.0,9.0,9.0],"snows":[9.0,8.0,7.0,7.0,8.0,7.0,7.0,6.0],"style1":8.0,"style2":8.0,"skills":[3.0,8.0,9.0,9.0,8.0],"radius":16.9,"tip":132,"mid":84,"tail":115,"link":"https://www.skiessentials.com/2024-ski-test/skis/2024-volkl-deacon-84"},{"ski":"Head e-Magnum (170cm)","length":170,"base":8.1,"forms":[9.0,9.0,7.0,5.0,6.0],"snows":[9.0,8.0,8.0,5.0,5.0,4.0,5.0,5.0],"style1":6.0,"style2":9.0,"skills":[4.0,9.0,9.0,7.0,6.0],"radius":13.1,"tip":126,"mid":72,"tail":110,"link":"https://blisterreview.com/gear-reviews/ski-reviews/2025-2026-head-supershape-e-magnum-e-rally-e-titan"},{"ski":"Stöckli Montero AS (172cm)","length":172,"base":8.0,"forms":[9.0,9.0,8.0,6.0,7.0],"snows":[9.0,8.0,8.0,6.0,6.0,5.0,6.0,6.0],"style1":8.0,"style2":7.0,"skills":[2.0,7.0,8.0,9.0,8.0],"radius":14.8,"tip":127,"mid":76,"tail":107,"link":"https://www.skiessentials.com/2025-ski-test/skis/2025-stockli-montero-as"},{"ski":"Head e-Race Pro (170cm)","length":170,"base":7.9,"forms":[5.0,7.0,9.0,8.0,6.0],"snows":[9.0,10.0,10.0,4.0,5.0,3.0,4.0,3.0],"style1":10.0,"style2":3.0,"skills":[1.0,3.0,5.0,8.0,10.0],"radius":14.8,"tip":117,"mid":68,"tail":102,"link":"https://www.head.com/en/product/worldcup-rebels-e-race-pro-313255-set"},{"ski":"Extrem PT 84 (170cm)","length":170,"base":8.3,"forms":[6.0,7.0,9.0,9.0,8.0],"snows":[9.0,9.0,8.0,8.0,8.0,6.0,6.0,5.0],"style1":8.0,"style2":8.0,"skills":[2.0,7.0,8.0,8.0,7.0],"radius":17.0,"tip":124,"mid":84,"tail":112,"link":"https://www.freeride.se/prylar/skidor/extrem-pt-84.html"},{"ski":"Völkl Deacon 84 V-Werks (177cm)","length":177,"base":8.6,"forms":[5.0,7.0,9.0,10.0,7.0],"snows":[10.0,9.0,9.0,7.0,8.0,6.0,6.0,5.0],"style1":9.0,"style2":6.0,"skills":[1.0,4.0,7.0,9.0,10.0],"radius":15.0,"tip":132,"mid":84,"tail":115,"link":"https://www.freeride.se/prylar/skidor/volkl-deacon-84-v-werks.html"},{"ski":"Black Crows Sora (172cm)","length":172,"base":8.5,"forms":[9.0,9.0,8.0,6.0,5.0],"snows":[9.0,9.0,8.0,7.0,8.0,5.0,7.0,6.0],"style1":9.0,"style2":7.0,"skills":[2.0,6.0,8.0,9.0,8.0],"radius":16.0,"tip":124,"mid":82,"tail":110,"link":"https://www.xxl.no/black-crows-alpine-skis-sora-allmountainski-hvit/p/1225990_1_Style"},{"ski":"Black Crows Sora (177cm)","length":177,"base":8.5,"forms":[8.0,9.0,8.0,7.0,5.0],"snows":[9.0,9.0,8.0,7.0,8.0,5.0,8.0,7.0],"style1":9.0,"style2":7.0,"skills":[2.0,5.0,7.0,9.0,9.0],"radius":17.0,"tip":124,"mid":82,"tail":110,"link":"https://www.xxl.no/black-crows-alpine-skis-sora-allmountainski-hvit/p/1225990_1_Style"},{"ski":"Elan Ripstick 96 Black Edition (175cm)","length":175,"base":8.4,"forms":[5.0,7.0,8.0,8.0,8.0],"snows":[7.0,7.0,6.0,8.0,8.0,9.0,8.0,7.0],"style1":8.0,"style2":7.0,"skills":[1.0,5.0,8.0,9.0,9.0],"radius":16.3,"tip":133,"mid":96,"tail":114,"link":"https://elansports.com/en-us/products/ripstick-96-black-edition"},{"ski":"Armada Declivity 82 Ti 2026 (174cm)","length":174,"base":8.2,"forms":[8.0,9.0,8.0,6.0,6.0],"snows":[9.0,10.0,10.0,6.0,6.0,4.0,5.0,4.0],"style1":9.0,"style2":6.0,"skills":[1.0,5.0,8.0,9.0,9.0],"radius":15.9,"tip":128,"mid":82,"tail":110,"link":"https://www.armadaskis.com/en-us/shop/product/declivity-82-ti-ra0624.html"},{"ski":"Head Kore 99 2022 (177cm)","length":177,"base":8.0,"forms":[5.0,6.0,8.0,8.0,7.0],"snows":[7.0,7.0,6.0,8.0,8.0,9.0,9.0,8.0],"style1":8.0,"style2":7.0,"skills":[1.0,4.0,7.0,9.0,9.0],"radius":17.0,"tip":134,"mid":99,"tail":120,"link":"https://www.skiessentials.com/2025-ski-test/skis/2025-head-kore-99"},{"ski":"Atomic Backland 107 (175cm)","length":175,"base":7.8,"forms":[4.0,5.0,7.0,7.0,7.0],"snows":[5.0,5.0,4.0,7.0,8.0,10.0,9.0,9.0],"style1":7.0,"style2":6.0,"skills":[2.0,5.0,7.0,8.0,9.0],"radius":17.2,"tip":137,"mid":107,"tail":124,"link":"https://www.atomic.com/en-se/shop-emea/product/backland-107-aa6292.html"},{"ski":"Elan Wingman 86 CTI 2023 (172cm)","length":172,"base":8.3,"forms":[7.0,8.0,9.0,9.0,7.0],"snows":[9.0,8.0,8.0,7.0,7.0,6.0,7.0,6.0],"style1":8.0,"style2":8.0,"skills":[2.0,7.0,9.0,9.0,8.0],"radius":15.6,"tip":130,"mid":86,"tail":115,"link":"https://www.skiessentials.com/2023-ski-test/skis/2023-elan-wingman-86-cti"},{"ski":"Stöckli Laser WRT PRO (172cm)","length":172,"base":8.7,"forms":[10.0,10.0,9.0,7.0,5.0],"snows":[9.0,10.0,10.0,5.0,5.0,3.0,4.0,3.0],"style1":10.0,"style2":4.0,"skills":[1.0,4.0,7.0,9.0,10.0],"radius":14.8,"tip":119,"mid":67,"tail":101,"link":"https://www.stoeckli.ch/usen/ski/ski/race/laser-wrt-pro"},{"ski":"Elan Primetime 55 Black Edition (172cm)","length":172,"base":8.4,"forms":[7.0,9.0,8.0,6.0,5.0],"snows":[9.0,10.0,10.0,6.0,6.0,4.0,5.0,4.0],"style1":7.0,"style2":8.0,"skills":[1.0,4.0,7.0,9.0,10.0],"radius":15.1,"tip":121,"mid":69,"tail":102,"link":"https://elansports.com/en-eu/products/primetime-55-black-edition"}],"turns":["Kort","Kort–medium","Medium","Medium–lang","Variabel"],"snows":["preparerte","harde","isete","slush","variert","løs snø 15cm","oppkjørt","hauger"],"styles":["Aktivt","Cruise","Hip to the snow"],"skills":["Beginner","Intermediate","Intermediate - Advanced","Advanced","Expert"],"skillAss":["Advanced","Expert"],"slopes":["Blå","Rød","Svart"],"widths":["Smal","Medium","Bred"],"slopeMap":{"Blå":4.0,"Rød":7.0,"Svart":9.0},"widthMap":{"Smal":4.0,"Medium":6.0,"Bred":8.0},"defaults":{"slope":"Svart","width":"Medium","snow":"harde","style":"Aktivt","skill":"Advanced","turn":"Kort–medium"}};
const APP_STATE = { limit: APP_DATA.rows.length };
const SOFT_SNOWS = new Set(["oppkjørt", "hauger", "slush", "variert", "løs snø 15cm"]);

function gid(id) { return document.getElementById(id); }
function fmt(n, d=2) { return Number(n).toLocaleString('no-NO', { minimumFractionDigits:d, maximumFractionDigits:d }); }
function fmt1(n) { return Number(n).toLocaleString('no-NO', { minimumFractionDigits:1, maximumFractionDigits:1 }); }
function round2(n) { return Math.round((Number(n)+Number.EPSILON)*100)/100; }
function round1(n) { return Math.round((Number(n)+Number.EPSILON)*10)/10; }
function clamp(n, min, max) { return Math.max(min, Math.min(max, n)); }
function isSoftSnow(v) { return SOFT_SNOWS.has(v); }
function esc(v) { return String(v ?? '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;'); }

function fillSelect(id, items, selected) {
  gid(id).innerHTML = items.map(v => `<option value="${esc(v)}"${v===selected?' selected':''}>${esc(v)}</option>`).join('');
}

function populateControls() {
  fillSelect('slope', APP_DATA.slopes, APP_DATA.defaults.slope);
  fillSelect('width', APP_DATA.widths, APP_DATA.defaults.width);
  fillSelect('snow', APP_DATA.snows, APP_DATA.defaults.snow);
  fillSelect('style', APP_DATA.styles, APP_DATA.defaults.style);
  fillSelect('turn', APP_DATA.turns, APP_DATA.defaults.turn);
  updateSkillOptions();
  gid('skill').value = APP_DATA.defaults.skill;
}

function updateSkillOptions() {
  const style = gid('style').value;
  const skillSel = gid('skill');
  const current = skillSel.value;
  const allowed = style === 'Hip to the snow' ? APP_DATA.skillAss : APP_DATA.skills;
  skillSel.innerHTML = allowed.map(v => `<option value="${esc(v)}"${v===current?' selected':''}>${esc(v)}</option>`).join('');
  if (!allowed.includes(skillSel.value)) skillSel.value = allowed[0];
  gid('skillHint').textContent = style === 'Hip to the snow'
    ? '🔥 Kun Advanced og Expert er tilgjengelige for valgt stil.'
    : '✅ Alle vanlige skill-nivåer er tilgjengelige for valgt stil.';
}

function getState() {
  return {
    slope: gid('slope').value,
    width: gid('width').value,
    snow: gid('snow').value,
    style: gid('style').value,
    skill: gid('skill').value,
    turn: gid('turn').value
  };
}

function scoreAll(state) {
  const formIdx = APP_DATA.turns.indexOf(state.turn);
  const snowIdx = APP_DATA.snows.indexOf(state.snow);
  const styleIdx = APP_DATA.styles.indexOf(state.style);
  const skillKey = state.style === 'Hip to the snow'
    ? APP_DATA.skillAss.indexOf(state.skill) + 4
    : APP_DATA.skills.indexOf(state.skill) + 1;

  const weights = isSoftSnow(state.snow)
    ? { base:0.16, form:0.08, snow:0.44, style:0.08, skill:0.24 }
    : { base:0.20, form:0.20, snow:0.28, style:0.08, skill:0.24 };

  const steep = APP_DATA.slopeMap[state.slope];
  const breadth = APP_DATA.widthMap[state.width];
  const mult = (state.snow==='preparerte'||state.snow==='harde'||state.snow==='isete') ? 1.05
    : (state.snow==='oppkjørt'||state.snow==='variert') ? 0.9
    : (state.snow==='slush'||state.snow==='hauger'||state.snow==='løs snø 15cm') ? 0.75
    : 0.85;
  const ideal = round1(clamp(((steep*0.7)+(breadth*0.3))*mult, 1, 10));
  const logic = state.slope === 'Blå'
    ? 'Slakere løype: modellen demper racing-/langradius-bias.'
    : (state.slope === 'Rød'
      ? 'Middels bratt løype: balanserer carvinggrep, stabilitet og allround-egenskaper.'
      : 'Brattere løype: mer stabile og mer aggressive carvingski kan vektes opp.');

  const ranking = APP_DATA.rows.map((row, idx) => {
    const form = Number(row.forms[formIdx]);
    const snowScore = Number(row.snows[snowIdx]);
    const aggro = round2((Math.max(Number(row.forms[3]), Number(row.forms[4]))*0.45) + (Number(row.style1)*0.35) + ((11-Number(row.style2))*0.2));
    const styleScore = styleIdx === 2
      ? Math.round(Math.min(10, (Number(row.style1)*0.7) + (aggro*0.3) + (form*0.15)))
      : (styleIdx === 1 ? Number(row.style2) : Number(row.style1));
    const skillScore = Number(row.skills[skillKey-1]);
    const weighted = round2((Number(row.base)*weights.base) + (form*weights.form) + (snowScore*weights.snow) + (styleScore*weights.style) + (skillScore*weights.skill));
    let penalty;
    if (isSoftSnow(state.snow)) {
      penalty = Math.abs(Number(row.mid)-96)*0.06 + Math.abs(Number(row.radius)-16.5)*0.04;
    } else if (state.style==='Hip to the snow' && (state.snow==='harde'||state.snow==='isete') && (state.slope==='Rød'||state.slope==='Svart')) {
      penalty = Math.abs(Number(row.mid)-69)*0.12 + Math.abs(Number(row.radius)-14.5)*0.18 - (Number(row.mid)<=69 ? 0.8 : 0);
    } else {
      penalty = Math.abs(Number(row.mid)-82)*0.03;
    }
    const bakke = round2(clamp(10 - (Math.abs(aggro-ideal)*0.85) - penalty, 1, 10));
    const final = round2((weighted*(isSoftSnow(state.snow)?0.78:0.55)) + (bakke*(isSoftSnow(state.snow)?0.22:0.45)));
    return {
      rank: 0,
      ski: row.ski,
      length: Number(row.length),
      radius: Number(row.radius),
      tip: Number(row.tip),
      mid: Number(row.mid),
      tail: Number(row.tail),
      link: row.link || '#',
      form: Math.round(form),
      snowScore: Math.round(snowScore),
      styleScore: Math.round(styleScore),
      skillScore: Math.round(skillScore),
      bakke,
      weighted,
      final,
      _sort: final + ((idx+16)/100000.0)
    };
  }).sort((a,b)=>b._sort-a._sort).map((it, i) => ({ ...it, rank:i+1 }));

  ranking.forEach(it => delete it._sort);
  return { winner: ranking[0], ideal, steep, breadth, logic, ranking, top3: ranking.slice(0,3) };
}

function buildWhyText(state, scored) {
  const w = scored.winner;
  const second = scored.ranking[1];
  const third = scored.ranking[2];
  return `Anbefalt ski: ${w.ski}
Lengde: ${w.length} cm
Score: ${fmt(w.final)}

${w.ski} ender øverst fordi modellen gir den beste totalmatchen for ${state.slope.toLowerCase()} løype, ${state.width.toLowerCase()} bredde, ${state.snow} forhold, ${state.style.toLowerCase()} stil og ${state.turn.toLowerCase()} svinger.

Sammenlignet med nr. 2 (${second.ski}) vinner den på totalbalansen mellom svingprofil, snøgrep, stilmatch og bakke-fit. Sammenlignet med nr. 3 (${third.ski}) taper ikke vinneren like mye på kompromisser i dette scenarioet.

Tolkning: Resultatlisten viser hvor godt hver ski matcher akkurat dette valget – ikke en universell fasit.`;
}

function updateWinner(scored, state) {
  const w = scored.winner;
  gid('winnerName').textContent = w.ski;
  gid('winnerMeta').textContent = `${w.length} cm • Radius ${fmt1(w.radius)} m • Midje ${w.mid} mm`;
  gid('winnerScore').textContent = fmt(w.final);
  gid('winnerContextLine').textContent = `${state.slope} løype • ${state.width} trase • ${state.snow} • ${state.style} • ${state.skill} • ${state.turn}`;
  gid('idealCarving').textContent = `${fmt1(scored.ideal)} / 10`;
  gid('slopeWidthProfile').textContent = `Bratthet ${scored.steep} / Bredde ${scored.breadth}`;
  gid('logicText').textContent = scored.logic;
  gid('winnerLink').href = w.link || '#';
  gid('whyWinner').textContent = buildWhyText(state, scored);

  const meterMap = [
    ['Turn', 'barTurn', 'valTurn', w.form],
    ['Snø', 'barSnow', 'valSnow', w.snowScore],
    ['Stil', 'barStyle', 'valStyle', w.styleScore],
    ['Ferdighet', 'barSkill', 'valSkill', w.skillScore],
    ['Bakke', 'barBakke', 'valBakke', w.bakke]
  ];
  meterMap.forEach(([, barId, valId, value]) => {
    gid(barId).style.width = `${Math.max(0, Math.min(100, Number(value)*10))}%`;
    gid(valId).textContent = fmt1(value);
  });
}

function renderTop3(scored) {
  const medals = ['🥇 #1 anbefaling','🥈 #2 anbefaling','🥉 #3 anbefaling'];
  gid('topCards').innerHTML = scored.top3.map((it, idx) => `
    <article class="top-card">
      <span class="badge">${medals[idx]}</span>
      <div class="model">${esc(it.ski)}</div>
      <div class="line">Lengde: ${it.length} cm • Score: ${fmt(it.final)}</div>
      <div class="line">Turn ${it.form} · Snø ${it.snowScore} · Stil ${it.styleScore} · Ferdighet ${it.skillScore} · Bakke ${fmt1(it.bakke)}</div>
      <div><a class="btn btn-secondary small" href="${esc(it.link)}" target="_blank" rel="noopener">Produktlenke</a></div>
    </article>
  `).join('');
}

function renderRanking(scored) {
  const q = (gid('search').value || '').toLowerCase();
  const rows = scored.ranking.filter(it => (`${it.ski} ${it.length} ${it.final} ${it.radius} ${it.mid}`).toLowerCase().includes(q)).slice(0, APP_STATE.limit);
  gid('rankingBody').innerHTML = rows.map(it => `
    <tr>
      <td><span class="rank-chip">${it.rank}</span></td>
      <td><strong>${esc(it.ski)}</strong></td>
      <td>${it.length} cm</td>
      <td>${fmt(it.final)}</td>
      <td>${fmt1(it.radius)}</td>
      <td>${it.mid}</td>
      <td class="why-mini">Turn ${it.form} · Snø ${it.snowScore} · Stil ${it.styleScore} · Ferdighet ${it.skillScore} · Bakke ${fmt1(it.bakke)}</td>
      <td><a href="${esc(it.link)}" target="_blank" rel="noopener">Lenke</a></td>
    </tr>
  `).join('');
}

function render() {
  const state = getState();
  const scored = scoreAll(state);
  updateWinner(scored, state);
  renderTop3(scored);
  renderRanking(scored);
  syncQueryFromState(state);
}

function setLimit(limit) {
  APP_STATE.limit = limit;
  gid('show5').classList.toggle('active', limit===5);
  gid('show10').classList.toggle('active', limit===10);
  gid('showAll').classList.toggle('active', limit===APP_DATA.rows.length);
  render();
}

function resetAll() {
  gid('slope').value = APP_DATA.defaults.slope;
  gid('width').value = APP_DATA.defaults.width;
  gid('snow').value = APP_DATA.defaults.snow;
  gid('style').value = APP_DATA.defaults.style;
  updateSkillOptions();
  gid('skill').value = APP_DATA.defaults.skill;
  gid('turn').value = APP_DATA.defaults.turn;
  gid('search').value = '';
  setLimit(APP_DATA.rows.length);
}

function syncQueryFromState(state) {
  const url = new URL(window.location.href);
  Object.entries(state).forEach(([k,v]) => url.searchParams.set(k, v));
  history.replaceState(null, '', url);
}

function restoreStateFromQuery() {
  const url = new URL(window.location.href);
  const q = Object.fromEntries(url.searchParams.entries());
  if (APP_DATA.slopes.includes(q.slope)) gid('slope').value = q.slope;
  if (APP_DATA.widths.includes(q.width)) gid('width').value = q.width;
  if (APP_DATA.snows.includes(q.snow)) gid('snow').value = q.snow;
  if (APP_DATA.styles.includes(q.style)) gid('style').value = q.style;
  updateSkillOptions();
  const allowed = gid('style').value === 'Hip to the snow' ? APP_DATA.skillAss : APP_DATA.skills;
  if (allowed.includes(q.skill)) gid('skill').value = q.skill;
  if (APP_DATA.turns.includes(q.turn)) gid('turn').value = q.turn;
}

async function copyShareLink() {
  try {
    await navigator.clipboard.writeText(window.location.href);
    const btn = gid('shareStateBtn');
    const old = btn.textContent;
    btn.textContent = 'Lenke kopiert';
    setTimeout(() => btn.textContent = old, 1400);
  } catch {
    alert('Kunne ikke kopiere lenken automatisk. Kopier URL-en fra adressefeltet.');
  }
}

function bind() {
  ['slope','width','snow','style','skill','turn','search'].forEach(id => {
    gid(id).addEventListener('change', render);
    gid(id).addEventListener('input', render);
  });
  gid('style').addEventListener('change', () => { updateSkillOptions(); render(); });
  gid('resetBtn').addEventListener('click', resetAll);
  gid('shareStateBtn').addEventListener('click', copyShareLink);
  gid('show5').addEventListener('click', () => setLimit(5));
  gid('show10').addEventListener('click', () => setLimit(10));
  gid('showAll').addEventListener('click', () => setLimit(APP_DATA.rows.length));
}

populateControls();
restoreStateFromQuery();
bind();
render();
